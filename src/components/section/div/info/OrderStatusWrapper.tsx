import styled from "styled-components";
import { Grid } from "../GridBox";

const OrderStatusWrapper = styled(Grid)`
    grid-template-columns: 1fr;
    align-content: space-between;
    height: auto;
`;

export { OrderStatusWrapper };